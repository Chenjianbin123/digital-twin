# 护士站场景左移设计

## 目标

将护士站 3D 场景整体向左移动，为右侧界面留出更合适的视觉空间。

## 调整范围

- 仅移动护士站 3D 场景层，不移动左下病区信息和“进入病房走廊”按钮。
- 桌面端基础左移 `32px`，移动端基础左移 `16px`。
- 基础偏移与现有鼠标跟随偏移叠加，保留原交互效果。

## 验收标准

- 护士站 3D 模型在桌面端向左移动 `32px`。
- 宽度不超过 `760px` 时向左移动 `16px`。
- 鼠标跟随、病区信息及走廊入口行为不变。
- 回归检查和生产构建通过。
