# 移除高清护士站外围长廊

## 目标

从护士站场景中永久移除截图所示的前端生成顶墙、灯板、墙面装饰和两块导向牌，只保留高清护士站 GLB 主体。

## 实现范围

- 删除 `AreaScene` 中额外创建的 `nurse-station-hospital-environment` 及其生命周期字段。
- 保留走廊场景复用的标牌纹理与墙面标牌方法，不影响病房走廊安全标识。
- 保留护士站主体、动态信息屏挂载、相机控制和数据刷新逻辑。
- 不修改病房走廊 `hospital-in.glb`、六扇门匹配、门口屏解析和病房内部场景。

## 兼容与清理

- 更新护士站场景边界测试，断言不再声明、创建或销毁这层生成环境。
- 保持模型文件及加载地址不变，不触发无关的模型缓存更新。
- 删除环境层后检查护士站默认镜头；仅在主体被裁切或构图明显偏移时调整镜头参数。

## 验证

- 源码边界检查确认 `nurse-station-hospital-environment` 不再创建，高清模型及动态屏幕挂载仍存在。
- 前端相关测试和生产构建通过。
- 在桌面浏览器中切换到护士站，确认只显示高清护士站主体，截图所示顶墙和两块标牌不再出现。

## 边界

本次不修改 Blender/GLB 资产、不压缩贴图、不降低模型精度，也不调整病房场景的模型、交互或数据权限。
